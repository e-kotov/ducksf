# Validate/select variables, enforce numeric, apply na.rm, infer type

Validate/select variables, enforce numeric, apply na.rm, infer type

## Usage

``` r
ducksf_interpolate_aw_plan_vars(source_sf, extensive, intensive, na.rm = FALSE)
```
