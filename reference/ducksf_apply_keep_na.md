# Apply keep_NA semantics (drop targets with all-NA on requested vars)

Apply keep_NA semantics (drop targets with all-NA on requested vars)

## Usage

``` r
ducksf_apply_keep_na(out, output = c("sf", "tibble"), vars_used, keep_NA)
```
